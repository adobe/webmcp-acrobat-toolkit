{
  "_comment": "Copy this file to .env.json (gitignored — never commit real values) and fill in what you use. All fields are optional; every provider also works by setting these same values directly via localStorage in the side panel's console, without a .env.json at all.",

  "apiKey": "",
  "model": "gemini-3.6-flash",

  "_comment_gateway": "Optional: any OpenAI-compatible gateway (e.g. an internal LLM gateway, OpenAI, OpenRouter, a local LiteLLM proxy). Set localStorage.aiProvider = 'gateway' to use it.",
  "gatewayKey": "",
  "gatewayBaseUrl": "",
  "gatewayModel": ""
}
